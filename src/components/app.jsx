// @flow strict

import * as React from 'react';

type Props = {||};

class app extends React.Component<Props> {
    render() {
        return (
            <div>
                
            </div>
        );
    }
}

export default app;